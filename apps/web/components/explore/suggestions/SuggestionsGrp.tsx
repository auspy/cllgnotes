const SuggestionsGrp = () => {
  // get all filters
  // allow users to select or remove filters on click
  // allow users to move to next filter grp
  // allow users to submit to apply filters
  return <></>;
};

export default SuggestionsGrp;
